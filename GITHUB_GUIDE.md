# Инструкция по выгрузке на GitHub

## 📋 Содержание
1. [Подготовка](#подготовка)
2. [Создание репозитория](#создание-репозитория)
3. [Загрузка файлов](#загрузка-файлов)
4. [Проверка](#проверка)
5. [Обновление кода](#обновление-кода)

---

## 🔧 Подготовка

### Шаг 1: Установка Git

**Windows:**
1. Скачайте Git с [git-scm.com](https://git-scm.com/download/win)
2. Запустите установщик
3. Оставьте настройки по умолчанию, нажимайте "Next"
4. Установите

**Проверка установки:**
```bash
git --version
```

### Шаг 2: Настройка Git

Откройте командную строку или PowerShell и выполните:

```bash
# Укажите ваше имя
git config --global user.name "Ваше Имя"

# Укажите ваш email (тот что на GitHub)
git config --global user.email "your-email@example.com"

# Установите кодировку
git config --global core.autocrlf true
```

### Шаг 3: Создание аккаунта GitHub (если нет)

1. Перейдите на [github.com](https://github.com)
2. Нажмите "Sign up"
3. Следуйте инструкциям регистрации

---

## 📦 Создание репозитория

### Вариант A: Через веб-интерфейс (Проще)

1. **Зайдите на GitHub**
   - Откройте [github.com](https://github.com)
   - Войдите в аккаунт

2. **Создайте новый репозиторий**
   - Нажмите "+" в правом верхнем углу
   - Выберите "New repository"

3. **Заполните форму:**
   - **Repository name:** `polymarket-price-logger` (или свое название)
   - **Description:** `Service for monitoring Polymarket prices 24/7`
   - **Visibility:**
     - ✅ **Public** - если хотите поделиться с другими
     - ⚠️ **Private** - если хотите держать в секрете
   - **НЕ ставьте галочки на:**
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
   - Нажмите **"Create repository"**

4. **Скопируйте URL репозитория**
   - На следующей странице найдите URL вида:
   - `https://github.com/ваш-username/polymarket-price-logger.git`
   - Скопируйте его

---

## 📤 Загрузка файлов

### Шаг 1: Откройте командную строку в папке проекта

**Windows:**
1. Откройте проводник
2. Перейдите в `C:\Poly\Price_logger`
3. В адресной строке напишите `cmd` и нажмите Enter

**Или в PowerShell:**
```powershell
cd C:\Poly\Price_logger
```

### Шаг 2: Инициализация Git

```bash
git init
```

Вы должны увидеть: `Initialized empty Git repository in...`

### Шаг 3: Добавление файлов

```bash
# Добавить все файлы
git add .

# Проверить что добавилось
git status
```

Вы увидите список зеленых файлов - это файлы готовые к коммиту.

### Шаг 4: Создание коммита

```bash
git commit -m "Initial commit: Polymarket Price Monitor Service"
```

### Шаг 5: Подключение к GitHub

Замените `ваш-username` и `название-репо` на ваши:

```bash
git remote add origin https://github.com/ваш-username/polymarket-price-logger.git
```

**Пример:**
```bash
git remote add origin https://github.com/john-doe/polymarket-price-logger.git
```

### Шаг 6: Отправка на GitHub

```bash
git branch -M main
git push -u origin main
```

**Важно:** При первой отправке Git попросит авторизацию:
- Введите ваш GitHub username
- Вместо пароля используйте **Personal Access Token** (см. ниже)

### Шаг 7: Создание Personal Access Token (если нужно)

Если Git просит пароль:

1. Перейдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Дайте название: `Polymarket Logger`
4. Выберите срок действия: `No expiration` (или любой другой)
5. Поставьте галочку на **`repo`** (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **ВАЖНО:** Скопируйте токен и сохраните! Он больше не отобразится
8. Используйте этот токен вместо пароля при `git push`

---

## ✅ Проверка

1. **Перейдите на GitHub**
   - Откройте `https://github.com/ваш-username/polymarket-price-logger`

2. **Проверьте файлы**
   - Вы должны увидеть все файлы проекта
   - README.md должен отображаться внизу страницы

3. **Готово!** 🎉
   - Ваш код на GitHub
   - Теперь можно деплоить на Railway, Render и т.д.

---

## 🔄 Обновление кода (после изменений)

Когда вы измените файлы локально и хотите обновить на GitHub:

```bash
# 1. Проверить что изменилось
git status

# 2. Добавить изменения
git add .

# 3. Создать коммит с описанием изменений
git commit -m "Описание изменений"

# 4. Отправить на GitHub
git push
```

**Примеры сообщений коммита:**
```bash
git commit -m "Add new market to config"
git commit -m "Fix price logging bug"
git commit -m "Update documentation"
git commit -m "Add Telegram notifications"
```

---

## 📝 Пошаговая инструкция (копировать-вставлять)

Откройте PowerShell в `C:\Poly\Price_logger` и выполните по порядку:

```powershell
# 1. Инициализация
git init

# 2. Добавление файлов
git add .

# 3. Создание коммита
git commit -m "Initial commit: Polymarket Price Monitor Service"

# 4. Подключение к GitHub (ЗАМЕНИТЕ НА СВОЙ URL!)
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# 5. Переименование ветки
git branch -M main

# 6. Отправка на GitHub
git push -u origin main
```

После выполнения последней команды Git попросит авторизацию - введите username и Personal Access Token.

---

## ⚠️ Важные моменты

### Что НЕ будет загружено (благодаря .gitignore):

- ✅ `logs/` - директория с данными
- ✅ `__pycache__/` - кэш Python
- ✅ Тестовые файлы
- ✅ Старые данные

### Что БУДЕТ загружено:

- ✅ Все `.py` файлы
- ✅ Документация (`.md` файлы)
- ✅ Конфигурация (`config.json`, `config.example.json`)
- ✅ Файлы для деплоя (`Procfile`, `Dockerfile`, и т.д.)
- ✅ `.gitignore`

### Безопасность:

**НЕ загружайте на GitHub:**
- ❌ Пароли
- ❌ API ключи
- ❌ Токены
- ❌ Приватные данные

Если случайно загрузили, удалите и измените пароли/токены!

---

## 🐙 Продвинутые команды

### Клонирование репозитория на другой компьютер:

```bash
git clone https://github.com/ваш-username/polymarket-price-logger.git
cd polymarket-price-logger
pip install -r requirements.txt
```

### Просмотр истории изменений:

```bash
git log
```

### Откат изменений:

```bash
# Отменить изменения в файле
git checkout -- filename.py

# Откатить последний коммит (НЕ отправленный на GitHub)
git reset --soft HEAD~1
```

### Создание ветки для экспериментов:

```bash
# Создать и переключиться на новую ветку
git checkout -b experimental

# Вернуться на main
git checkout main

# Слить изменения из ветки
git merge experimental
```

---

## 🆘 Частые проблемы

### Проблема: "fatal: not a git repository"
**Решение:** Выполните `git init` в правильной папке

### Проблема: "remote origin already exists"
**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/ваш-username/репо.git
```

### Проблема: "failed to push some refs"
**Решение:**
```bash
git pull origin main --allow-unrelated-histories
git push origin main
```

### Проблема: "Authentication failed"
**Решение:** Используйте Personal Access Token вместо пароля

### Проблема: "Author identity unknown"
**Решение:**
```bash
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

---

## 📱 GitHub Desktop (Альтернатива)

Если не хотите использовать командную строку:

1. **Скачайте GitHub Desktop**
   - [desktop.github.com](https://desktop.github.com/)

2. **Установите и войдите**
   - Запустите приложение
   - Войдите в GitHub аккаунт

3. **Добавьте репозиторий**
   - File → Add local repository
   - Выберите `C:\Poly\Price_logger`
   - Нажмите "Create repository"

4. **Загрузите на GitHub**
   - Напишите описание коммита
   - Нажмите "Commit to main"
   - Нажмите "Publish repository"

5. **Готово!**
   - Дальнейшие изменения: Commit → Push

---

## ✅ Чеклист для GitHub

- [ ] Git установлен
- [ ] Git настроен (username, email)
- [ ] Аккаунт на GitHub создан
- [ ] Репозиторий на GitHub создан
- [ ] URL репозитория скопирован
- [ ] `git init` выполнен
- [ ] `git add .` выполнен
- [ ] `git commit` выполнен
- [ ] `git remote add origin` выполнен
- [ ] `git push` выполнен
- [ ] Файлы видны на GitHub
- [ ] README отображается

**После выполнения всех пунктов - проект на GitHub!** 🎉

---

## 🚀 Следующие шаги

После загрузки на GitHub:

1. **Деплой на Railway**
   - См. `DEPLOY_QUICK.md`
   - Railway → New Project → Deploy from GitHub

2. **Деплой на Render**
   - См. `DEPLOY_QUICK.md`
   - Render → New → Background Worker → Connect GitHub

3. **Настройка автоматического деплоя**
   - Каждый `git push` будет автоматически обновлять сервис в облаке

4. **Создание красивого README**
   - Добавьте badges (статус деплоя, версия Python, и т.д.)
   - Добавьте скриншоты
   - См. [shields.io](https://shields.io)

---

## 📞 Дополнительная помощь

**Официальная документация:**
- [Git](https://git-scm.com/doc)
- [GitHub Docs](https://docs.github.com)

**Обучающие ресурсы:**
- [GitHub Skills](https://skills.github.com/)
- [Learn Git Branching](https://learngitbranching.js.org/)

**Проблемы с Git?**
- См. `TROUBLESHOOTING.md` в проекте
