# Быстрое развертывание в облаке - 3 простых варианта

## 🥇 Вариант 1: Railway (Самый простой - 5 минут)

### Преимущества:
- ✅ Проще простого
- ✅ $5 бесплатных кредитов в месяц
- ✅ Автоматический деплой из GitHub
- ✅ Встроенные логи и мониторинг

### Шаги:

1. **Загрузите проект на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/polymarket-logger.git
   git push -u origin main
   ```

2. **Зарегистрируйтесь на Railway**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

3. **Создайте проект**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит Python и запустит!

4. **Готово!**
   - Сервис работает 24/7
   - Логи доступны в Dashboard
   - Данные сохраняются в `logs/`

**Стоимость:** ~$5/месяц (или бесплатно в пределах кредитов)

---

## 🥈 Вариант 2: Oracle Cloud (Полностью бесплатно навсегда!)

### Преимущества:
- ✅ Навсегда бесплатно
- ✅ 2 VM с 1GB RAM
- ✅ 200GB хранилища
- ✅ Полный контроль

### Шаги:

1. **Регистрация**
   - Перейдите на [oracle.com/cloud/free](https://www.oracle.com/cloud/free/)
   - Зарегистрируйтесь (потребуется кредитная карта, но НЕ списывается)

2. **Создание VM**
   - В консоли: Compute → Instances → Create Instance
   - Выберите: "Always Free eligible" (VM.Standard.E2.1.Micro)
   - OS: Ubuntu 22.04
   - Создайте SSH ключ или используйте существующий
   - Создайте

3. **Подключение к серверу**
   ```bash
   ssh ubuntu@your_vm_ip
   ```

4. **Установка**
   ```bash
   # Обновление системы
   sudo apt update && sudo apt upgrade -y

   # Установка Python и Git
   sudo apt install python3 python3-pip git -y

   # Клонирование проекта (или загрузка через scp)
   git clone https://github.com/your-username/polymarket-logger.git
   cd polymarket-logger

   # Установка зависимостей
   pip3 install -r requirements.txt

   # Настройка config.json
   nano config.json
   # (отредактируйте, Ctrl+O сохранить, Ctrl+X выйти)
   ```

5. **Запуск как служба**
   ```bash
   # Создание systemd службы
   sudo nano /etc/systemd/system/polymarket.service
   ```

   Вставьте:
   ```ini
   [Unit]
   Description=Polymarket Monitor
   After=network.target

   [Service]
   Type=simple
   User=ubuntu
   WorkingDirectory=/home/ubuntu/polymarket-logger
   ExecStart=/usr/bin/python3 price_monitor_service.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```

   Запустите:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable polymarket
   sudo systemctl start polymarket

   # Проверка статуса
   sudo systemctl status polymarket
   ```

6. **Готово!**
   - Сервис работает 24/7 бесплатно
   - Просмотр логов: `sudo journalctl -u polymarket -f`
   - Доступ к файлам: `ls ~/polymarket-logger/logs/`

**Стоимость:** $0 навсегда!

---

## 🥉 Вариант 3: DigitalOcean (Лучшее соотношение цена/простота)

### Преимущества:
- ✅ Очень простая настройка
- ✅ Отличная документация
- ✅ $200 кредитов для новых пользователей
- ✅ Надежность и скорость

### Шаги:

1. **Регистрация**
   - Перейдите на [digitalocean.com](https://www.digitalocean.com)
   - Зарегистрируйтесь (получите $200 кредитов на 60 дней)

2. **Создание Droplet**
   - Create → Droplets
   - Выберите:
     - Ubuntu 22.04 LTS
     - Basic - Regular - $4/мес (512MB)
     - Datacenter: ближайший регион
   - Authentication: SSH keys (рекомендуется) или Password
   - Create Droplet

3. **Подключение**
   ```bash
   ssh root@your_droplet_ip
   ```

4. **Установка** (те же команды что и для Oracle, но от root)
   ```bash
   # Установка
   apt update && apt upgrade -y
   apt install python3 python3-pip git -y

   # Загрузка проекта
   mkdir /opt/polymarket-logger
   cd /opt/polymarket-logger

   # Загрузите файлы через scp:
   # scp -r C:\Poly\Price_logger/* root@your_droplet_ip:/opt/polymarket-logger/

   # Или клонируйте из GitHub:
   # git clone https://github.com/your-username/polymarket-logger.git .

   pip3 install -r requirements.txt
   nano config.json
   ```

5. **Запуск как служба**
   ```bash
   nano /etc/systemd/system/polymarket.service
   ```

   ```ini
   [Unit]
   Description=Polymarket Monitor
   After=network.target

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/opt/polymarket-logger
   ExecStart=/usr/bin/python3 price_monitor_service.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

   ```bash
   systemctl daemon-reload
   systemctl enable polymarket
   systemctl start polymarket
   systemctl status polymarket
   ```

**Стоимость:** $4/месяц (или бесплатно с $200 кредитами)

---

## 📊 Сравнение

| Критерий | Railway | Oracle Cloud | DigitalOcean |
|----------|---------|--------------|--------------|
| **Простота** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Время настройки** | 5 мин | 20 мин | 15 мин |
| **Цена** | $5/мес | $0 | $4/мес |
| **Контроль** | Ограниченный | Полный | Полный |
| **Надежность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

## 🎯 Мои рекомендации

**Вы новичок?** → **Railway**
- Самый простой
- Работает через 5 минут
- Не нужно разбираться с серверами

**Хотите бесплатно?** → **Oracle Cloud**
- Навсегда бесплатно
- Требует времени на настройку
- Полный контроль

**Нужен баланс?** → **DigitalOcean**
- $4/мес (дешевле кофе)
- Простая настройка
- Отличная документация
- $200 кредитов для старта

---

## 🔍 Проверка работы

### Railway
```
Dashboard → Deployments → Logs
```

### Oracle Cloud / DigitalOcean
```bash
# SSH подключение
ssh ubuntu@your_ip  # или root@your_ip

# Статус службы
sudo systemctl status polymarket

# Логи в реальном времени
sudo journalctl -u polymarket -f

# Просмотр файлов
ls logs/
cat logs/market-name_2026-01-18.json
```

---

## 📥 Скачивание логов

### Railway
Через CLI или веб-интерфейс

### Oracle Cloud / DigitalOcean
```bash
# С сервера на ваш компьютер
scp -r ubuntu@your_ip:~/polymarket-logger/logs ./backup

# Или через SFTP клиент (WinSCP, FileZilla)
```

---

## 💡 Pro Tips

1. **Регулярный backup**
   - Скачивайте логи раз в неделю
   - Используйте `rsync` для инкрементального бэкапа

2. **Мониторинг**
   - Проверяйте логи раз в день
   - Настройте уведомления (Telegram bot)

3. **Безопасность**
   - Используйте SSH ключи (не пароли)
   - Регулярно обновляйте систему

4. **Оптимизация**
   - Удаляйте старые логи (> 30 дней)
   - Сжимайте архивные данные

---

## 🆘 Помощь

**Проблемы с Railway?**
- Проверьте логи в Dashboard
- Убедитесь что `Procfile` и `railway.json` на месте

**Проблемы с VPS?**
- Проверьте статус: `systemctl status polymarket`
- Проверьте логи: `journalctl -u polymarket -n 50`
- Проверьте файлы: `ls -la /opt/polymarket-logger/`

**Общие проблемы?**
- См. `TROUBLESHOOTING.md`
- Проверьте `config.json` на ошибки
- Убедитесь что `requests` установлен

---

## ✅ Итоговый чеклист

- [ ] Выбрана платформа
- [ ] Аккаунт создан
- [ ] Проект загружен
- [ ] Сервис запущен
- [ ] Логи работают
- [ ] Данные записываются
- [ ] Настроен автозапуск
- [ ] Протестирован backup

**После выполнения всех пунктов - готово!** 🎉

Ваш сервис работает в облаке 24/7 и собирает данные о ценах Polymarket!
